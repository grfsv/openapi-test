package com.streview.presentation.controller


{{#operations}}
{{#operation}}
{{#-first}}
import com.streview.application.usecases.{{classVarName}}.*
import com.streview.application.usecases.{{classVarName}}.dto.*
{{/-first}}
{{/operation}}
{{/operations}}
import io.ktor.http.*
import io.ktor.http.content.*
import io.ktor.server.auth.*
import io.ktor.server.request.*
import io.ktor.server.response.*
import io.ktor.server.routing.*
import io.ktor.utils.io.*
import kotlinx.io.Source
import org.koin.ktor.ext.inject

{{#operations}}
fun Route.{{classVarName}}Controller() {
{{#operation}}
        {{httpMethod.toLowerCase}}("{{path}}") {
            // jwtを解析して得られたuserIDを取得
            val userID = call.principal<UserIdPrincipal>()!!.name

            TODO("実装する")
        }
{{/operation}}
    }
{{/operations}}